[70. 爬楼梯 - 力扣（LeetCode）](https://leetcode.cn/problems/climbing-stairs/description/)



```java
class Solution {
    public int climbStairs(int n) {
        // 动态规划数组：dp[i] 表示到达第 i 阶楼梯的方法数
        int[] dp = new int[n + 1];
        
        // 处理边界情况
        if (n == 1) {
            return 1;  // 只有1阶楼梯，只有1种方法
        }
        if (n == 2) {
            return 2;  // 有2阶楼梯，有2种方法（1+1 或 2）
        }
        
        // 初始化基础情况
        dp[1] = 1;  // 到达第1阶：1种方法（爬1阶）
        dp[2] = 2;  // 到达第2阶：2种方法（爬1阶两次 或 爬2阶一次）
        
        // 动态规划递推：从第3阶开始计算
        for (int i = 3; i <= n; i++) {
            // 状态转移方程：
            // 到达第i阶的方法数 = 到达第i-1阶的方法数 + 到达第i-2阶的方法数
            // 因为可以从第i-1阶爬1阶到达，或者从第i-2阶爬2阶到达
            dp[i] = dp[i - 1] + dp[i - 2];
        }
        
        // 返回到达第n阶的方法数
        return dp[n];
    }
}
```

[746. 使用最小花费爬楼梯 - 力扣（LeetCode）](https://leetcode.cn/problems/min-cost-climbing-stairs/description/)

- 

```java
class Solution {
    public int minCostClimbingStairs(int[] cost) {
        int n = cost.length; // 获取台阶总数
        
        // 动态规划数组：f[i] 表示到达第 i 个台阶（包括顶部）的最小花费
        int[] f = new int[n + 1];
        
        // 初始情况：到达第0阶或第1阶的花费为0，因为我们可以直接从这两阶开始
        // 注意：f[0]和f[1]默认为0，因为数组初始化为0
        
        // 从第2阶开始计算，直到到达顶部（第n阶）
        for (int i = 2; i <= n; i++) {
            // 状态转移方程：到达第i阶的最小花费有两种可能：
            // 1. 从第i-1阶迈一步上来，花费 = 到达i-1阶的最小花费 + 从i-1阶迈步的代价
            // 2. 从第i-2阶迈两步上来，花费 = 到达i-2阶的最小花费 + 从i-2阶迈步的代价
            // 取两者中的较小值
            f[i] = Math.min(f[i - 1] + cost[i - 1], f[i - 2] + cost[i - 2]);
        }
        
        // 返回到达顶部（第n阶）的最小花费
        return f[n];
    }
}
```

[3693. 爬楼梯 II - 力扣（LeetCode）](https://leetcode.cn/problems/climbing-stairs-ii/description/)



```java
class Solution {
    public int climbStairs(int n, int[] costs) {
        // 动态规划数组：f[i] 表示到达第 i 个台阶（并支付该台阶费用）的最小总花费
        int[] f = new int[n + 1];
        
        // 从第1个台阶开始计算
        for (int i = 1; i <= n; i++) {
            // 初始化当前台阶的最小花费为最大值
            int res = Integer.MAX_VALUE;
            
            // 考虑从前面的台阶跳过来，最多可以向前跳3个台阶
            // 所以从距离当前台阶不超过3个的台阶中，寻找最小花费
            for (int j = Math.max(i - 3, 0); j < i; j++) {
                // 计算从第j个台阶跳到第i个台阶的总花费：
                // 到达第j个台阶的最小花费 + 跳跃距离的平方
                res = Math.min(res, f[j] + (i - j) * (i - j));
            }
            
            // 到达第i个台阶的总花费 = 最小跳跃花费 + 第i个台阶本身的费用
            f[i] = res + costs[i - 1];
        }
        
        // 返回到达第n个台阶的最小总花费
        return f[n];
    }
}
```

[377. 组合总和 Ⅳ - 力扣（LeetCode）](https://leetcode.cn/problems/combination-sum-iv/description/)

- 

```java
class Solution {
    public int combinationSum4(int[] nums, int target) {
        // dp数组，dp[i]表示和为i的组合数
        int[] dp = new int[target + 1];
        
        // 边界条件：和为0只有一种组合（不选任何数）
        dp[0] = 1;
        
        // 动态规划：计算从1到target的所有组合数
        for (int i = 1; i <= target; i++) {
            // 遍历数组中的每个数
            for (int num : nums) {
                // 如果当前数小于等于目标和i，可以将其加入组合
                if (num <= i) {
aryl                    // 状态转移：组合数等于使用当前数之前的组合数之和
                    dp[i] += dp[i - num];
                }
            }
        }
        
        // 返回目标和为target的组合数
        return dp[target];
    }
}
```

[2466. 统计构造好字符串的方案数 - 力扣（LeetCode）](https://leetcode.cn/problems/count-ways-to-build-good-strings/)

- 

```java
class Solution {
    public int countGoodStrings(int low, int high, int zero, int one) {
        // 定义模数，避免结果溢出
        final int MOD = 1_000_000_007;
        int ans = 0; // 存储最终结果
        
        // dp数组，f[i]表示构造长度为i的好字符串的方案数
        int[] f = new int[high + 1];
        
        // 初始化：长度为0的字符串（空串）只有1种方案
        f[0] = 1;
        
        // 动态规划计算长度为1到high的所有方案
        for (int i = 1; i <= high; i++) {
            // 如果可以在字符串末尾添加zero个'0'字符
            if (i >= zero) {
                f[i] = f[i - zero];
            }
            
            // 如果可以在字符串末尾添加one个'1'字符
            if (i >= one) {
                f[i] = (f[i] + f[i - one]) % MOD;
            }
            
            // 如果当前长度在[low, high]范围内，累加到结果中
            if (i >= low) {
                ans = (ans + f[i]) % MOD;
            }
        }
        
        return ans;
    }
}
```

[2266. 统计打字方案数 - 力扣（LeetCode）](https://leetcode.cn/problems/count-number-of-texts/description/)

- 

```java
class Solution {
    // 定义模数，用于取模防止溢出
    private static final int MOD = 1_000_000_007;
    // 定义最大可能长度，根据题目约束设置
    private static final int MX = 100_001;
    // f[i]: 对于3字母按键，连续按i次同一键可生成的字母序列数（步长限制为1,2,3）
    private static final long[] f = new long[MX];
    // g[i]: 对于4字母按键（7和9），连续按i次同一键可生成的字母序列数（步长限制为1,2,3,4）
    private static final long[] g = new long[MX];
    // 标记预计算是否已完成，避免重复计算
    private static boolean done = false;

    // 初始化函数，预计算f和g数组
    // 使用延迟初始化，只在第一次调用时计算
    private void init() {
        if (done) {
            return;  // 如果已经计算过，直接返回
        }
        done = true;  // 标记为已计算
        
        // 初始化基础情况
        f[0] = g[0] = 1;  // 长度为0时，只有1种方案（空序列）
        f[1] = g[1] = 1;  // 长度为1时，只有1种方案（按1次键）
        f[2] = g[2] = 2;  // 长度为2时，有2种方案：(1,1)或(2)
        f[3] = g[3] = 4;  // 长度为3时，有4种方案
        
        // 动态规划计算所有可能长度的方案数
        for (int i = 4; i < MX; i++) {
            // 对于3字母按键：f[i] = f[i-1] + f[i-2] + f[i-3]
            // 表示最后一步可以按1次、2次或3次键
            f[i] = (f[i - 1] + f[i - 2] + f[i - 3]) % MOD;
            
            // 对于4字母按键：g[i] = g[i-1] + g[i-2] + g[i-3] + g[i-4]
            // 表示最后一步可以按1次、2次、3次或4次键
            g[i] = (g[i - 1] + g[i - 2] + g[i - 3] + g[i - 4]) % MOD;
        }
    }

    public int countTexts(String S) {
        // 初始化预计算数组
        init();
        
        long ans = 1;  // 最终结果，使用long类型避免中间结果溢出
        int cnt = 0;   // 当前连续相同数字的计数
        
        char[] s = S.toCharArray();  // 转换为字符数组便于处理
        
        // 遍历整个字符串
        for (int i = 0; i < s.length; i++) {
            char c = s[i];
            cnt++;  // 增加当前数字的连续计数
            
            // 如果当前字符是最后一个，或者与下一个字符不同，则到达一个连续段的结尾
            if (i == s.length - 1 || c != s[i + 1]) {
                // 根据当前数字是7/9还是其他数字，选择相应的方案数
                // 如果不是7或9，使用f[cnt]（3字母按键）
                // 如果是7或9，使用g[cnt]（4字母按键）
                ans = ans * (c != '7' && c != '9' ? f[cnt] : g[cnt]) % MOD;
                
                // 重置计数器，准备统计下一个连续段
                cnt = 0;
            }
        }
        
        // 将结果转换为int类型返回
        return (int) ans;
    }
}
```

§1.2 打家劫舍
答疑
问：在 1:1 翻译的过程中，如何根据记忆化搜索，确定递推数组（DP 数组）的大小？为什么有时候要开 n+1 大小的数组，有时候要开 n+2 大小的数组？

答：看记忆化搜索的参数的范围（最小值和最大值）。例如 i 最小是 −1（递归边界也算），最大是 n−1（递归入口），那么一共有 n+1 个不同的 i，就需要开 n+1 大小的 DP 数组。如果 i 最小是 −2，最大是 n−1，一共有 n+2 个不同的 i，就需要开

[198. 打家劫舍 - 力扣（LeetCode）](https://leetcode.cn/problems/house-robber/description/)

- 

```java
class Solution {
    public int rob(int[] nums) {
        // 处理边界情况：数组为空或长度为0，返回0
        if (nums == null || nums.length == 0) {
            return 0;
        }
        
        int length = nums.length;
        
        // 只有一个房屋时，只能抢劫这个房屋
        if (length == 1) {
            return nums[0];
        }
        
        // 动态规划状态定义：
        // first 表示 dp[i-2]，即到前前个房屋为止的最大金额
        // second 表示 dp[i-1]，即到前一个房屋为止的最大金额
        int first = nums[0];  // dp[0] = nums[0]
        int second = Math.max(nums[0], nums[1]);  // dp[1] = max(nums[0], nums[1])
        
        // 从第三个房屋开始遍历
        for (int i = 2; i < length; i++) {
            // 临时保存second的当前值，用于更新first
            int temp = second;
            
            // 状态转移方程：
            // dp[i] = max(dp[i-1], dp[i-2] + nums[i])
            // 即当前房屋可以选择抢或不抢：
            // 1. 不抢当前房屋：金额保持为 dp[i-1]（即second）
            // 2. 抢当前房屋：金额为 dp[i-2] + nums[i]（即first + nums[i]）
            second = Math.max(first + nums[i], second);
            
            // 更新first为上一次的second（即dp[i-1]）
            first = temp;
        }
        
        // 返回最后一个房屋的最优解
        return second;
    }
}
```

[213. 打家劫舍 II - 力扣（LeetCode）](https://leetcode.cn/problems/house-robber-ii/description/)

- 

```java
class Solution {
    public int rob(int[] nums) {
        int length = nums.length;
        
        // 边界情况处理
        if (length == 1) {
            return nums[0];
        } else if (length == 2) {
            return Math.max(nums[0], nums[1]);
        }
        
        // 由于房屋是环形的，首尾不能同时抢劫，所以有两种情况：
        // 1. 抢劫第一个到倒数第二个房屋（排除最后一个房屋）
        // 2. 抢劫第二个到最后一个房屋（排除第一个房屋）
        // 取两种情况的最大值
        return Math.max(robRange(nums, 0, length - 2), robRange(nums, 1, length - 1));
    }

    // 计算在给定区间[start, end]内抢劫的最大金额
    public int robRange(int[] nums, int start, int end) {
        // 动态规划状态定义：
        // first 表示 dp[i-2]，即到前前个房屋为止的最大金额
        // second 表示 dp[i-1]，即到前一个房屋为止的最大金额
        
        // 初始化前两个房屋的状态
        int first = nums[start];
        int second = Math.max(nums[start], nums[start + 1]);
        
        // 从第三个房屋开始遍历
        for (int i = start + 2; i <= end; i++) {
            // 临时保存second的当前值，用于更新first
            int temp = second;
            
            // 状态转移方程：
            // dp[i] = max(dp[i-1], dp[i-2] + nums[i])
            // 即当前房屋可以选择抢或不抢：
            // 1. 不抢当前房屋：金额保持为 dp[i-1]（即second）
            // 2. 抢当前房屋：金额为 dp[i-2] + nums[i]（即first + nums[i]）
            second = Math.max(first + nums[i], second);
            
            // 更新first为上一次的second（即dp[i-1]）
            first = temp;
        }
        
        // 返回给定区间内的最大抢劫金额
        return second;
    }
}
```

[2320. 统计放置房子的方式数 - 力扣（LeetCode）](https://leetcode.cn/problems/count-number-of-ways-to-place-houses/description/)



```java
class Solution {
    // 定义模数，用于取模防止溢出
    private static final int MOD = 1_000_000_007;
    // 定义最大可能的地块数
    private static final int MX = 10_001;
    // 预计算数组，f[i] 表示街道一边长度为i时放置房屋的方案数
    private static final int[] f = new int[MX];

    // 静态初始化块，在类加载时执行，预计算f数组
    static {
        // 边界条件初始化
        f[0] = 1;  // 长度为0时，只有1种方案（不放房屋）
        f[1] = 2;  // 长度为1时，有2种方案：放房屋或不放
        
        // 状态转移方程：f[i] = f[i-1] + f[i-2]
        // 解释：
        // 1. 不在第i个地块放房屋：方案数等于前i-1个地块的方案数 f[i-1]
        // 2. 在第i个地块放房屋：则第i-1个地块不能放房屋，方案数等于前i-2个地块的方案数 f[i-2]
        for (int i = 2; i < MX; i++) {
            f[i] = (f[i - 1] + f[i - 2]) % MOD;
        }
    }

    public int countHousePlacements(int n) {
        // 街道有两边，两边独立，总方案数 = 一边的方案数 × 另一边的方案数
        // 由于两边对称，总方案数 = f[n] × f[n] = f[n]²
        // 使用long类型防止乘法溢出
        return (int) ((long) f[n] * f[n] % MOD);
    }
}
```

[3186. 施咒的最大总伤害 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-total-damage-with-spell-casting/description/)



```java
class Solution {
    public long maximumTotalDamage(int[] power) {
        // 统计每个能量值出现的次数
        Map<Integer, Integer> cnt = new HashMap<>();
        for (int x : power) {
            cnt.merge(x, 1, Integer::sum); // 等价于 cnt.put(x, cnt.getOrDefault(x, 0) + 1)
        }

        int n = cnt.size();
        int[] a = new int[n]; // 存储不重复的能量值
        int k = 0;
        for (int x : cnt.keySet()) {
            a[k++] = x; // 将所有不重复的能量值存入数组
        }
        Arrays.sort(a); // 对能量值进行排序

        // 动态规划数组，f[i]表示考虑前i个能量值时能获得的最大伤害
        long[] f = new long[n + 1];
        
        int j = 0; // 指针，用于找到可以与当前能量值同时选择的上一个能量值
        for (int i = 0; i < n; i++) {
            int x = a[i]; // 当前能量值
            
            // 移动j指针，找到最后一个与x差值大于2的能量值
            // 因为如果选择x，那么与x差值<=2的能量值都不能选择
            while (a[j] < x - 2) {
                j++;
            }
            
            // 状态转移方程：
            // 1. 不选择当前能量值x：f[i]（从f[i]继承）
            // 2. 选择当前能量值x：f[j] + (x的伤害总和)
            // 取两者较大值
            f[i + 1] = Math.max(f[i], f[j] + (long) x * cnt.get(x));
        }
        return f[n]; // 返回考虑所有能量值时的最大伤害
    }
}
```

[2140. 解决智力问题 - 力扣（LeetCode）](https://leetcode.cn/problems/solving-questions-with-brainpower/description/)



```java
class Solution {
    public long mostPoints(int[][] questions) {
        int n = questions.length;
        // 动态规划数组，f[i]表示从第i个问题到最后一个问题能获得的最大分数
        long[] f = new long[n + 1];
        
        // 从最后一个问题向前计算
        for (int i = n - 1; i >= 0; i--) {
            // 获取当前问题的分数和需要跳过的题目数
            int points = questions[i][0];      // 当前问题的分数
            int brainpower = questions[i][1];  // 解答当前问题后需要跳过的题目数
            
            // 计算解答当前问题后下一个可以解答的问题索引
            int j = Math.min(i + brainpower + 1, n);
            
            // 状态转移方程：
            // 1. 不解答当前问题：f[i+1]（跳转到下一个问题）
            // 2. 解答当前问题：points + f[j]（当前分数 + 跳过brainpower个问题后的最大分数）
            f[i] = Math.max(f[i + 1], f[j] + points);
        }
        
        // 返回从第一个问题开始能获得的最大分数
        return f[0];
    }
}
```

§1.3 最大子数组和（最大子段和）
有两种做法：

定义状态 f[i] 表示以 a[i] 结尾的最大子数组和，不和 i 左边拼起来就是 f[i]=a[i]，和 i 左边拼起来就是 f[i]=f[i−1]+a[i]，取最大值就得到了状态转移方程 f[i]=max(f[i−1],0)+a[i]，答案为 max(f)。这个做法也叫做 Kadane 算法。
用 前缀和，转化成 121. 买卖股票的最佳时机。

[53. 最大子数组和 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-subarray/description/)



```java
class Solution {
    public int maxSubArray(int[] nums) {
        // 动态规划数组，f[i] 表示以 nums[i] 结尾的最大子数组和
        int[] f = new int[nums.length];
        
        // 初始化：以第一个元素结尾的最大子数组和就是它本身
        f[0] = nums[0];
        
        // 初始化答案为第一个元素
        int ans = f[0];
        
        // 遍历数组，计算每个位置的最大子数组和
        for (int i = 1; i < nums.length; i++) {
            // 状态转移方程：
            // 以 nums[i] 结尾的最大子数组和 =
            // nums[i] + max(f[i-1], 0)
            // 即要么从当前位置重新开始，要么延续前一个位置的子数组
            f[i] = Math.max(f[i - 1], 0) + nums[i];
            
            // 更新全局最大子数组和
            ans = Math.max(ans, f[i]);
        }
        
        // 返回最大子数组和
        return ans;
    }
}
```

[2606. 找到最大开销的子字符串 - 力扣（LeetCode）](https://leetcode.cn/problems/find-the-substring-with-maximum-cost/description/)



```java
class Solution {
    public int maximumCostSubstring(String s, String chars, int[] vals) {
        // 创建字符到值的映射数组，初始值为默认值（a=1, b=2, ..., z=26）
        int[] mapping = new int[26];
        for (int i = 0; i < 26; i++) {
            mapping[i] = i + 1; // 默认值：'a'对应1，'b'对应2，...，'z'对应26
        }
        
        // 使用chars和vals更新特定字符的值
        for (int i = 0; i < vals.length; i++) {
            mapping[chars.charAt(i) - 'a'] = vals[i];
        }

        // 使用Kadane算法求最大子数组和
        // ans记录全局最大和，初始化为0（允许子串为空，空子串和为0）
        int ans = 0;
        // f表示以当前字符结尾的最大子数组和
        int f = 0;
        
        // 遍历字符串中的每个字符
        for (char c : s.toCharArray()) {
            // 状态转移方程：
            // 以当前字符结尾的最大子数组和 = max(以前一个字符结尾的最大子数组和, 0) + 当前字符的值
            // 即要么从当前字符重新开始一个子串，要么延续之前的子串
            f = Math.max(f, 0) + mapping[c - 'a'];
            
            // 更新全局最大和
            ans = Math.max(ans, f);
        }
        
        return ans;
    }
}
```

[1749. 任意子数组和的绝对值的最大值 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-absolute-sum-of-any-subarray/description/)



```java
class Solution {
    public int maxAbsoluteSum(int[] nums) {
        // 动态规划：同时跟踪最大子数组和和最小子数组和
        int ans = 0;        // 记录最大绝对和
        int fMax = 0;      // 以当前元素结尾的最大子数组和
        int fMin = 0;      // 以当前元素结尾的最小子数组和
        
        for (int x : nums) {
            // Kadane算法计算最大子数组和
            // 如果之前的子数组和为负，就重新开始
            fMax = Math.max(fMax, 0) + x;
            
            // 计算最小子数组和（用于处理负数绝对值大的情况）
            // 如果之前的子数组和为正，就重新开始
            fMin = Math.min(fMin, 0) + x;
            
            // 最大绝对和可能是：
            // 1. 最大子数组和（正数）
            // 2. 最小子数组和的绝对值（负数）
            ans = Math.max(ans, Math.max(fMax, -fMin));
        }
        
        return ans;
    }
}
```

[1191. K 次串联后最大子数组之和 - 力扣（LeetCode）](https://leetcode.cn/problems/k-concatenation-maximum-sum/description/)

 

```java
class Solution {
    // 计算重复多次数组的最大子数组和
    // repeat: 数组重复的次数
    private int maxSubArray(int[] nums, int repeat) {
        int n = nums.length;
        int ans = 0; // 允许子数组为空，所以初始化为0
        int f = 0;   // 以当前元素结尾的最大子数组和
        
        // 遍历重复后的整个数组
        for (int i = 0; i < n * repeat; i++) {
            // 获取当前元素（循环使用原数组）
            int x = nums[i % n];
            // 状态转移：要么从当前元素重新开始，要么延续之前的子数组
            f = Math.max(f, 0) + x;
            // 更新全局最大值
            ans = Math.max(ans, f);
        }
        return ans;
    }

    public int kConcatenationMaxSum(int[] arr, int k) {
        // 如果k=1，直接求原数组的最大子数组和
        if (k == 1) {
            return maxSubArray(arr, 1);
        }
        
        // 计算连接两个数组后的最大子数组和
        long ans = maxSubArray(arr, 2);
        
        // 计算原数组的总和
        int s = 0;
        for (int x : arr) {
            s += x;
        }
        
        // 如果总和s>0，中间可以插入(k-2)个完整的数组
        // 最终结果 = 连接两个数组的最大子数组和 + (k-2) * s
        ans += (long) Math.max(s, 0) * (k - 2);
        
        // 对结果取模
        return (int) (ans % 1_000_000_007);
    }
}
```

[918. 环形子数组的最大和 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-sum-circular-subarray/description/)



```java
class Solution {
    public int maxSubarraySumCircular(int[] nums) {
        // 初始化变量
        int maxF = 0;           // 以当前元素结尾的最大子数组和（空间优化）
        int maxS = Integer.MIN_VALUE; // 全局最大子数组和（不能为空子数组）
        int minF = 0;           // 以当前元素结尾的最小子数组和（空间优化）
        int minS = 0;           // 全局最小子数组和（可以为空子数组，和为0）
        int sum = 0;           // 数组所有元素的和
        
        // 遍历数组
        for (int x : nums) {
            // 计算以当前元素结尾的最大子数组和
            // Kadane算法：maxF = max(maxF + x, x) 等价于 max(maxF, 0) + x
            maxF = Math.max(maxF, 0) + x;
            // 更新全局最大子数组和
            maxS = Math.max(maxS, maxF);
            
            // 计算以当前元素结尾的最小子数组和
            // 类似Kadane算法，但求最小值：minF = min(minF + x, x) 等价于 min(minF, 0) + x
            minF = Math.min(minF, 0) + x;
            // 更新全局最小子数组和
            minS = Math.min(minS, minF);
            
            // 累加数组元素和
            sum += x;
        }
        
        // 返回结果：
        // 情况1：如果数组中所有元素都是负数（maxS < 0），则最大子数组和为maxS
        // 情况2：否则，最大子数组和可能是以下两者中的较大值：
        //       a) 不跨越边界的情况：maxS
        //       b) 跨越边界的情况：sum - minS （总和减去最小的子数组和，这个最小的子数组通常不跨越边界）
        return maxS < 0 ? maxS : Math.max(maxS, sum - minS);
    }
}
```

[2321. 拼接数组的最大分数 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-score-of-spliced-array/description/)



```java
class Solution {
    public int maximumsSplicedArray(int[] nums1, int[] nums2) {
        // 计算两种交换方式的最大值：
        // 1. 交换子数组使得nums1的总和最大
        // 2. 交换子数组使得nums2的总和最大
        return Math.max(solve(nums1, nums2), solve(nums2, nums1));
    }

    /**
     * 计算交换子数组后，使得nums1总和的最大值
     * @param nums1 目标数组，我们希望交换子数组后使其总和最大
     * @param nums2 源数组，用于与nums1交换子数组
     * @return 交换子数组后nums1可能达到的最大总和
     */
    private int solve(int[] nums1, int[] nums2) {
        int s1 = 0;       // nums1的原始总和
        int maxSum = 0;   // 差值数组的最大子数组和
        int f = 0;        // 以当前元素结尾的差值最大子数组和
        
        for (int i = 0; i < nums1.length; i++) {
            // 累加nums1的原始总和
            s1 += nums1[i];
            
            // 计算差值：nums2[i] - nums1[i]，表示用nums2[i]替换nums1[i]的收益
            // 使用Kadane算法计算差值数组的最大子数组和
            f = Math.max(f, 0) + nums2[i] - nums1[i];
            
            // 更新全局最大差值
            maxSum = Math.max(maxSum, f);
        }
        
        // 最终nums1的最大总和 = 原始总和 + 交换的最佳子数组带来的增益
        return s1 + maxSum;
    }
}
```

[152. 乘积最大子数组 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-product-subarray/description/)



```java
class Solution {
    public int maxProduct(int[] nums) {
        int n = nums.length;
        // fMax[i] 表示以第i个元素结尾的最大乘积子数组的乘积
        int[] fMax = new int[n];
        // fMin[i] 表示以第i个元素结尾的最小乘积子数组的乘积
        int[] fMin = new int[n];
        
        // 初始化：以第一个元素结尾的子数组只有一个元素
        fMax[0] = fMin[0] = nums[0];
        
        for (int i = 1; i < n; i++) {
            int x = nums[i];
            
            // 状态转移方程：
            // 以第i个元素结尾的最大乘积有以下三种可能：
            // 1. 将x加到以i-1结尾的最大乘积子数组后：fMax[i-1] * x
            // 2. 将x加到以i-1结尾的最小乘积子数组后：fMin[i-1] * x（当x为负数时，乘以最小的负数可能得到最大正数）
            // 3. 从x开始新的子数组：x
            fMax[i] = Math.max(Math.max(fMax[i - 1] * x, fMin[i - 1] * x), x);
            
            // 以第i个元素结尾的最小乘积同样有三种可能：
            // 1. 将x加到以i-1结尾的最大乘积子数组后：fMax[i-1] * x
            // 2. 将x加到以i-1结尾的最小乘积子数组后：fMin[i-1] * x
            // 3. 从x开始新的子数组：x
            fMin[i] = Math.min(Math.min(fMax[i - 1] * x, fMin[i - 1] * x), x);
        }
        
        // 在所有以i结尾的最大乘积中找出全局最大值
        return Arrays.stream(fMax).max().getAsInt();
    }
}
```

[1186. 删除一次得到子数组最大和 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-subarray-sum-with-one-deletion/description/)



```java
class Solution {
    public int maximumSum(int[] arr) {
        int n = arr.length;
        // 动态规划数组
        // f[i][0]: 以第i个元素结尾，没有删除任何元素的最大子数组和
        // f[i][1]: 以第i个元素结尾，已经删除一个元素的最大子数组和
        int[][] f = new int[n + 1][2];
        
        // 初始化：将f[0][0]和f[0][1]初始化为一个非常小的负数
        // 除以2防止在后续计算中溢出（当负数加上一个很小的负数时可能下溢）
        Arrays.fill(f[0], Integer.MIN_VALUE / 2);
        
        int ans = Integer.MIN_VALUE;  // 最终结果，初始化为最小整数
        
        for (int i = 0; i < n; i++) {
            // 状态转移方程：
            
            // 1. 以arr[i]结尾，没有删除任何元素的最大子数组和：
            //    要么延续前面的子数组（如果前面的子数组和为正），要么从arr[i]重新开始
            f[i + 1][0] = Math.max(f[i][0], 0) + arr[i];
            
            // 2. 以arr[i]结尾，已经删除一个元素的最大子数组和：
            //    情况1：前面已经删除了一个元素，当前元素必须保留，所以是f[i][1] + arr[i]
            //    情况2：前面没有删除元素，现在删除当前元素，所以是f[i][0]（跳过当前元素）
            f[i + 1][1] = Math.max(f[i][1] + arr[i], f[i][0]);
            
            // 更新全局最大和：考虑以arr[i]结尾的两种情况
            ans = Math.max(ans, Math.max(f[i + 1][0], f[i + 1][1]));
        }
        
        return ans;
    }
}
```

[3381. 长度可被 K 整除的子数组的最大元素和 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-subarray-sum-with-length-divisible-by-k/description/)



```java
class Solution {
    public long maxSubarraySum(int[] nums, int k) {
        int n = nums.length;
        // 前缀和数组，sum[i] 表示前i个元素的和
        // sum[0] = 0, sum[1] = nums[0], ..., sum[n] = nums[0] + ... + nums[n-1]
        long[] sum = new long[n + 1];
        for (int i = 0; i < n; i++) {
            sum[i + 1] = sum[i] + nums[i];
        }

        // minS[i] 表示在模k的余数为i的位置中，前缀和的最小值
        // 初始化一个很大的数，防止减法溢出
        long[] minS = new long[k];
        Arrays.fill(minS, Long.MAX_VALUE / 2);

        long ans = Long.MIN_VALUE;  // 最终结果，初始化为最小整数
        
        // 遍历前缀和数组
        for (int j = 0; j < sum.length; j++) {
            // 计算当前索引对k取模的结果
            int i = j % k;
            
            // 当前子数组和 = 当前前缀和 - 同余数位置的最小前缀和
            // 由于同余，差值对应的子数组长度是k的倍数
            ans = Math.max(ans, sum[j] - minS[i]);
            
            // 更新同余数位置的最小前缀和
            minS[i] = Math.min(minS[i], sum[j]);
        }
        return ans;
    }
}
```

二、网格图 DP
对于一些二维 DP（例如背包、最长公共子序列），如果把 DP 矩阵画出来，其实状态转移可以视作在网格图上的移动。所以在学习相对更抽象的二维 DP 之前，做一些形象的网格图 DP 会让后续的学习更轻松（比如 0-1 背包的空间优化写法为什么要倒序遍历）。

[64. 最小路径和 - 力扣（LeetCode）](https://leetcode.cn/problems/minimum-path-sum/description/)



```java
class Solution {
    public int minPathSum(int[][] grid) {
        int m = grid.length;       // 网格的行数
        int n = grid[0].length;    // 网格的列数
        
        // 动态规划数组，f[i][j]表示从起点(0,0)到位置(i-1,j-1)的最小路径和
        // 使用(i+1,j+1)的索引是为了方便处理边界情况
        int[][] f = new int[m + 1][n + 1];
        
        // 初始化边界为最大值，表示这些位置不可达
        Arrays.fill(f[0], Integer.MAX_VALUE);
        for (int i = 0; i < m; i++) {
            f[i + 1][0] = Integer.MAX_VALUE;
        }
        
        // 遍历网格的每个单元格
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                // 如果是起点(0,0)，最小路径和就是该点的值
                if (i == 0 && j == 0) {
                    f[1][1] = grid[i][j];
                } else {
                    // 状态转移方程：
                    // 到达(i,j)的最小路径和 = 
                    // 当前格子的值 + min(从上方来的路径和, 从左方来的路径和)
                    f[i + 1][j + 1] = Math.min(f[i + 1][j], f[i][j + 1]) + grid[i][j];
                }
            }
        }
        
        // 返回到达右下角的最小路径和
        return f[m][n];
    }
}
```

[62. 不同路径 - 力扣（LeetCode）](https://leetcode.cn/problems/unique-paths/description/)



```java
class Solution {
    public int uniquePaths(int m, int n) {
        // 动态规划数组，f[i][j] 表示从起点到达网格(i-1, j-1)位置的路径数
        // 使用 (m+1) x (n+1) 的数组，方便处理边界情况
        int[][] f = new int[m + 1][n + 1];
        
        // 初始化：将 f[0][1] 设为 1，作为起点上方的一个虚拟位置
        // 这样在计算 f[1][1] 时，f[0][1] + f[1][0] = 1 + 0 = 1
        // 相当于从起点出发只有一条路径
        f[0][1] = 1;
        
        // 遍历网格的每个位置
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                // 状态转移方程：
                // 到达 (i,j) 的路径数 = 从上方来的路径数 + 从左方来的路径数
                // 注意：f数组的下标比实际网格下标大1
                f[i + 1][j + 1] = f[i][j + 1] + f[i + 1][j];
            }
        }
        
        // 返回到达右下角 (m-1, n-1) 的路径总数
        return f[m][n];
    }
}
```

[63. 不同路径 II - 力扣（LeetCode）](https://leetcode.cn/problems/unique-paths-ii/description/)

```java
class Solution {
    public int uniquePathsWithObstacles(int[][] obstacleGrid) {
        int m = obstacleGrid.length;       // 网格的行数
        int n = obstacleGrid[0].length;    // 网格的列数
        
        // 动态规划数组，f[i][j]表示从起点(0,0)到达网格位置(i-1,j-1)的路径数
        // 使用(m+1)×(n+1)的数组，方便处理边界情况
        int[][] f = new int[m + 1][n + 1];
        
        // 初始化：设置f[0][1] = 1，这是一个虚拟起点
        // 这样在计算f[1][1]时，f[1][1] = f[0][1] + f[1][0] = 1 + 0 = 1
        f[0][1] = 1;
        
        // 遍历网格的每个位置
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                // 如果当前格子不是障碍物，计算到达该格子的路径数
                if (obstacleGrid[i][j] == 0) {
                    // 状态转移方程：
                    // 到达(i,j)的路径数 = 从上方来的路径数 + 从左方来的路径数
                    f[i + 1][j + 1] = f[i][j + 1] + f[i + 1][j];
                }
                // 如果当前格子是障碍物，f[i+1][j+1]保持为0（默认值）
                // 表示无法到达这个格子
            }
        }
        
        // 返回到达右下角(m-1,n-1)的路径总数
        return f[m][n];
    }
}
```

[120. 三角形最小路径和 - 力扣（LeetCode）](https://leetcode.cn/problems/triangle/description/)



```java
class Solution {
    public int minimumTotal(List<List<Integer>> triangle) {
        int n = triangle.size();  // 三角形的行数
        
        // 动态规划数组，f[i][j] 表示从三角形第i行第j列出发到达底部的最小路径和
        int[][] f = new int[n][n];
        
        // 初始化：三角形的最后一行，从这些位置出发的最小路径和就是它们自身的值
        for (int j = 0; j < n; j++) {
            f[n - 1][j] = triangle.get(n - 1).get(j);
        }
        
        // 从倒数第二行开始向上计算
        for (int i = n - 2; i >= 0; i--) {
            // 第i行有i+1个元素
            for (int j = 0; j <= i; j++) {
                // 状态转移方程：
                // 从(i,j)出发的最小路径和 = 当前值 + 从下一行的相邻位置出发的最小路径和的较小值
                // 相邻位置：(i+1,j) 和 (i+1,j+1)
                f[i][j] = Math.min(f[i + 1][j], f[i + 1][j + 1]) + triangle.get(i).get(j);
            }
        }
        
        // 返回从顶部(0,0)出发的最小路径和
        return f[0][0];
    }
}
```

[3393. 统计异或值为给定值的路径数目 - 力扣（LeetCode）](https://leetcode.cn/problems/count-paths-with-the-given-xor-value/description/)

```java
class Solution {
    public int countPathsWithXorValue(int[][] grid, int k) {
        final int MOD = 1_000_000_007;  // 定义模数，防止结果过大
        
        // 1. 找出网格中的最大值
        int mx = 0;
        for (int[] row : grid) {
            for (int val : row) {
                mx = Math.max(mx, val);
            }
        }
        
        // 2. 计算异或结果的最大范围u
        // Integer.numberOfLeadingZeros(mx) 返回mx二进制表示中前导0的个数
        // 32 - numberOfLeadingZeros(mx) 得到表示mx所需的最小位数
        // u = 1 << (这个位数) 得到大于mx的最小的2的幂次
        // 这是为了限制异或结果的范围，因为异或值不会超过这个范围
        int u = 1 << (32 - Integer.numberOfLeadingZeros(mx));
        
        // 3. 如果k超过异或结果的可能范围，直接返回0
        if (k >= u) {
            return 0;
        }
        
        int m = grid.length;     // 网格行数
        int n = grid[0].length;  // 网格列数
        
        // 4. 动态规划数组
        // f[i][j][x] 表示从起点(0,0)到网格位置(i-1,j-1)的路径中，异或值为x的路径数
        // 使用(m+1)×(n+1)的数组，方便处理边界情况
        int[][][] f = new int[m + 1][n + 1][u];
        
        // 5. 初始化：起点之前的虚拟位置，异或值为0的路径有1条
        // 这样在计算f[1][1]时，可以从f[0][1]和f[1][0]转移过来
        f[0][1][0] = 1;
        
        // 6. 遍历网格的每个位置
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                int val = grid[i][j];  // 当前格子的值
                
                // 7. 遍历所有可能的异或值x
                for (int x = 0; x < u; x++) {
                    // 状态转移方程：
                    // 要想到达(i,j)时异或值为x，需要从上方或左方以异或值x^val到达
                    // 因为 (x^val) ^ val = x
                    // 从上方(i-1,j)到达：f[i][j+1][x^val]
                    // 从左方(i,j-1)到达：f[i+1][j][x^val]
                    f[i + 1][j + 1][x] = (f[i + 1][j][x ^ val] + f[i][j + 1][x ^ val]) % MOD;
                }
            }
        }
        
        // 8. 返回从起点到终点(m-1,n-1)的异或值为k的路径数
        return f[m][n][k];
    }
}

```

[931. 下降路径最小和 - 力扣（LeetCode）](https://leetcode.cn/problems/minimum-falling-path-sum/description/)

```java
class Solution {
    private int[][] memo;  // 记忆化数组，存储从每个单元格出发能到达的最大列索引
    
    public int maxMoves(int[][] grid) {
        int m = grid.length;
        int n = grid[0].length;
        memo = new int[m][n];
        int maxMoves = 0;
        
        // 从第一列的每个单元格出发
        for (int i = 0; i < m; i++) {
            maxMoves = Math.max(maxMoves, dfs(i, 0, grid));
        }
        
        return maxMoves;
    }
    
    private int dfs(int i, int j, int[][] grid) {
        int m = grid.length;
        int n = grid[0].length;
        
        // 如果已经计算过，直接返回结果
        if (memo[i][j] != 0) {
            return memo[i][j];
        }
        
        int maxCol = j;  // 当前能到达的最大列索引，初始化为当前列
        
        // 尝试向右上、右、右下三个方向移动
        int[] directions = {-1, 0, 1};  // 行偏移量
        
        for (int d : directions) {
            int newRow = i + d;
            int newCol = j + 1;
            
            // 检查新位置是否在网格内，且值大于当前位置
            if (newRow >= 0 && newRow < m && newCol < n && 
                grid[newRow][newCol] > grid[i][j]) {
                maxCol = Math.max(maxCol, dfs(newRow, newCol, grid));
            }
        }
        
        // 记忆化结果
        memo[i][j] = maxCol;
        return maxCol;
    }
}
```

[3603. 交替方向的最小路径代价 II - 力扣（LeetCode）](https://leetcode.cn/problems/minimum-cost-path-with-alternating-directions-ii/description/)

```java
class Solution {
    public long minCost(int m, int n, int[][] waitCost) {
        // 使用一维数组进行空间优化
        long[] dp = new long[n + 1];
        Arrays.fill(dp, Long.MAX_VALUE);
        
        // 初始化起点
        dp[1] = 1;  // 起点只需要额外时间1 * 1=1，等待时间被抵消
        
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (i == 0 && j == 0) {
                    // 起点已经初始化
                    dp[1] = 1;
                } else {
                    // 状态转移
                    dp[j + 1] = Math.min(dp[j + 1], dp[j]) 
                                 + waitCost[i][j] 
                                 + (long) (i + 1) * (j + 1);
                }
            }
        }
        
        // 返回结果，减去终点的等待时间
        return dp[n] - waitCost[m - 1][n - 1];
    }
}
```

[2304. 网格中的最小路径代价 - 力扣（LeetCode）](https://leetcode.cn/problems/minimum-path-cost-in-a-grid/)

```java
class Solution {
    public int minPathCost(int[][] grid, int[][] moveCost) {
        int m = grid.length;      // 网格行数
        int n = grid[0].length;   // 网格列数
        
        // 动态规划数组，f[i][j]表示从第i行第j列出发到达最后一行的最小总代价
        int[][] f = new int[m][n];
        
        // 初始化最后一行：从最后一行出发，只需要当前格子的值
        // 直接将最后一行grid[m-1]赋值给f[m-1]
        f[m - 1] = grid[m - 1];
        
        // 从倒数第二行开始向上计算
        for (int i = m - 2; i >= 0; i--) {
            for (int j = 0; j < n; j++) {
                // 初始化当前f[i][j]为最大值
                f[i][j] = Integer.MAX_VALUE;
                
                // 枚举下一行的所有列k，选择移动到下一行第k列
                for (int k = 0; k < n; k++) {
                    // 状态转移方程：
                    // 从(i,j)移动到下一行第k列的代价 = 
                    // 从(i+1,k)出发的最小代价 + 从(i,j)移动到(i+1,k)的移动代价
                    f[i][j] = Math.min(f[i][j], f[i + 1][k] + moveCost[grid[i][j]][k]);
                }
                
                // 加上当前格子的值
                f[i][j] += grid[i][j];
            }
        }
        
        // 从第一行的所有列中找出最小代价
        int ans = Integer.MAX_VALUE;
        for (int res : f[0]) {
            ans = Math.min(ans, res);
        }
        return ans;
    }
}
```

[1289. 下降路径最小和 II - 力扣（LeetCode）](https://leetcode.cn/problems/minimum-falling-path-sum-ii/description/)

```java
class Solution {
    public int minFallingPathSum(int[][] grid) {
        int n = grid.length;  // 网格大小
        int[][] d = new int[n][n];  // 动态规划数组，d[i][j]表示到达第i行第j列的最小路径和
        
        // 初始化d数组为最大值
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                d[i][j] = Integer.MAX_VALUE;
            }
        }
        
        // 初始化第一行
        for (int i = 0; i < n; i++) {
            d[0][i] = grid[0][i];
        }
        
        // 动态规划递推
        for (int i = 1; i < n; i++) {  // 从第二行开始
            for (int j = 0; j < n; j++) {  // 当前行的每一列
                for (int k = 0; k < n; k++) {  // 枚举上一行的每一列
                    if (j == k) {  // 不能在同一列
                        continue;
                    }
                    // 状态转移方程：
                    // 从上一行的第k列下降到当前行的第j列
                    d[i][j] = Math.min(d[i][j], d[i - 1][k] + grid[i][j]);
                }
            }
        }
        
        // 在最后一行中找到最小值
        int res = Integer.MAX_VALUE;
        for (int j = 0; j < n; j++) {
            res = Math.min(res, d[n - 1][j]);
        }
        return res;
    }
}
```

[3418. 机器人可以获得的最大金币数 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-amount-of-money-robot-can-earn/description/)

